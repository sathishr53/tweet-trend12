pipeline {
    agent {
        node {
            label 'maven'
        }
    }

    #stages {
     #   stage('clone-code') {
      #      steps {
       #         git branch: 'main', url: 'https://github.com/sathishr53/tweet-trend12'
        #    }
       # }
    #}
    environment {
        PATH = "/opt/apache-maven-3.9.11/bin:$PATH"
    }
stages {
      stage('build') {
           steps {
               sh 'mvn clean deploy'
           }
        }
    }


}